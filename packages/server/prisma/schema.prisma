generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Winner {
  id       String   @id @default(uuid())
  name     String
  datetime DateTime @default(now())
}
