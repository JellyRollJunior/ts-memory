generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Winner {
  id       String   @id @default(uuid())
  name     String
  datetime DateTime @default(now())
}
